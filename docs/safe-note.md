# Safe Note

This file is intentionally plain. It exists so the guard policies have harmless public content to scan.

The expected workflow is:

1. Normalize an agent request into a capability.
2. Evaluate runtime policy before action.
3. Run static checks before publication.
4. Keep reviewable evidence in tests and CI logs.
